{
  "name": "smetoplan",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "test": "tsx --test tests/golden/**/*.test.ts",
    "db:up": "podman compose -f db/podman-compose.yml up -d",
    "db:down": "podman compose -f db/podman-compose.yml down",
    "db:migrate": "node --env-file=.env.local scripts/db-migrate.mjs",
    "db:generate-pseo": "node --env-file=.env.local scripts/generate-pseo-routes.mjs",
    "pseo:quality-sync": "node --env-file=.env.local scripts/pseo-quality-sync.mjs",
    "pseo:drip": "npx tsx --env-file=.env.local scripts/pseo-drip-once.ts",
    "pseo:bootstrap": "node --env-file=.env.local scripts/pseo-bootstrap.mjs",
    "market:fetch": "node --env-file=.env.local scripts/fetch-market-prices.mjs",
    "cron:drip": "curl -s -H \"Authorization: Bearer %CRON_SECRET%\" http://localhost:3000/api/cron/drip-feed",
    "cron:market": "curl -s -H \"Authorization: Bearer %CRON_SECRET%\" http://localhost:3000/api/cron/market-prices"
  },
  "dependencies": {
    "lucide-react": "^1.27.0",
    "next": "15.5.22",
    "pdf-lib": "^1.17.1",
    "pg": "^8.22.0",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/pg": "^8.20.0",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.5.22",
    "tailwindcss": "^4",
    "tsx": "^4.23.1",
    "typescript": "^5"
  }
}
